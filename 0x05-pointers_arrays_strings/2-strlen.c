#include "main.h"
/**
 * _strlen(char *s)- returns the length of a string
 *
 * @s:entry char
 *
 * Return: lenth
 */
int _strlen(char *s)
{
	int lenth = 0;

	while (*s != '\0')
	{
		s++;
		len++;
	}
	return (len);
}
