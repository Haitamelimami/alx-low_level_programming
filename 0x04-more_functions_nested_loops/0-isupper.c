#include "main.h"
/**
 * _isupper(int c)- checks for uppercase character
 *
 * @c: entry to be tested
 *
 * Return: 1 if true 0 if false
 */
int _isupper(int c)
{
	if (c < 91 && c > 64)
	{
		return (1);
	}
	else
	{
		return (0);
	}
)
