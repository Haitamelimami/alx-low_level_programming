#include "main.h"
/**
 * _abs(int)-  computes the absolute value of an integer
 *
 * @n: number to be calculated
 *
 * Return: n if pos -n if nega
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (0);
	}
}

